% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunTradeSeqTocky.R
\name{RunTradeSeqTocky}
\alias{RunTradeSeqTocky}
\title{Run TradeSeq Differential Dynamics Analysis on Tocky Trajectory}
\usage{
RunTradeSeqTocky(
  object,
  gradient_res,
  group_by,
  genes,
  n_knots = 5,
  n_cores = 1
)
}
\arguments{
\item{object}{A Seurat object OR a sparse expression matrix (Genes x Cells).}

\item{gradient_res}{The data frame returned by \code{GradientTocky_Slerp} (must contain \verb{$angle}).}

\item{group_by}{If \code{object} is Seurat: Character string of the metadata column name (e.g., "treatment").
If \code{object} is Matrix: A named factor/character vector of groups matching column names.}

\item{genes}{Character vector of genes to test (e.g., output of \code{SelectTockyGenes}).}

\item{n_knots}{Integer. Number of knots for the GAM (default 5).}

\item{n_cores}{Integer. Number of cores for parallel processing (default 1).}
}
\value{
A list containing:
\item{results}{Data frame of differential dynamics results (p-values, FDR).}
\item{gam_fit}{The raw tradeSeq GAM object (for downstream plotting).}
}
\description{
A wrapper function to perform differential dynamic expression testing using tradeSeq.
Supports Seurat v5+ (uses 'layer') and matrix inputs.
}
